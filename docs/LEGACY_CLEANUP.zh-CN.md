# 旧版遗留清理

日期：2026-09-14。清理内容随 0.0.3 交付。

## 已移出当前项目

- `runtime/host`：旧 `0.1.0-rc.7` 内嵌运行时及旧插件，不是当前系统运行环境。
- `out/nsis`：旧 0.0.1 安装包、更新清单和校验文件，避免误发旧包。
- 旧 macOS 截图：换成当前 Windows 关于页面截图。
- 三份历史插件审计文件及空的临时目录。
- 十份较早的研究输出已归档；本轮验证记录保留。

项目归档保存在仓库外的本地归档目录，不随源码上传。

## 已移出运行目录

- 损坏的旧 node_modules，包括已失效的 Firefly 和旧插件链接；当前 web 依赖已重建。
- `profiles/node_modules` 中的旧父级依赖，避免模块解析回落到旧安装。
- 空的 `.dsh-module-fallback` 目录和名为 `18` 的旧 pnpm 错误日志。
- 本次修复的配置备份已转移到独立归档，不留在活动 profile 内。
- pnpm 工作区中仅针对旧 rc/alpha 版本的安装年龄例外已清除，其他配置保留。

用户数据备份保存在用户主目录下的 `.minke-archive`，不随源码上传。

## 有意保留

- 用户会话、凭据、模型设置、工作区和主动安装的 dshmarket。
- `~/.minke`、`@firefly-harness/bundle-companion` 等兼容标识及迁移检测清单；改名会造成数据或插件配置不兼容。
- 上游源码、许可证、版权和历史发布记录。
- 依赖锁文件中的上游兼容版本范围：其中出现旧版本号不代表实际安装了旧版本，不改写第三方依赖声明。
- 其他独立项目、品牌素材库，以及全局 Volta/Node/pnpm 安装；本项目通过私有命令路径隔离损坏的全局包装器，不擅自卸载其他工具。

归档为可恢复保留，不代表永久删除。仓库与当前运行目录不再使用归档内容。

## 清理后验证

233 项测试通过，类型检查和 Windows x64 打包通过；移走旧父级依赖后，实际桌面启动、设置页回归和隔离插件添加/移除验证通过。
