# 纯净桌面范围

MyDSH 0.0.1 默认不安装独立附加插件；上游内置模块和“插件”设置页保留。
桌面适配 bundle 保留标题栏、本地模型、终端、文件与网页功能，以及自定义图标。主界面名称由上游提供，不再替换。

“设置 → 插件市场管理”可按需安装或更新官方 `dshmarket`；不预装，也不移除用户主动安装的市场。安装失败事务回滚，安装期间退出会等待事务完成。

软件名称为 MyDSH；内部 bundle ID 和 `~/.minke` 数据路径为兼容已有安装保留，不代表继续显示旧软件品牌。
仅检查桌面软件更新，不检查上游 Harness 更新。当前运行与构建基线为 `0.1.5-rc.2`。

详细修复、测试及边界见 [项目状态](PROJECT_STATUS.zh-CN.md)。
