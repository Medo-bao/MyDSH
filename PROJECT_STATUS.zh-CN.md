# MyDSH 项目状态

版本：0.0.1。更新时间：2026-09-13。

## 产品范围

- Windows x64 桌面端，仓库为 https://github.com/Medo-bao/MyDSH。
- 上游 Harness 固定为 `0.1.5-rc.2`，提交 `fb2c4b9e698e30edb738bca4cf0618587db7d203`，子模块无本地补丁。
- 保留上游主界面名称和插件设置页。MyDSH 使用自定义图标、标题栏、本地模型设置、终端、网页面板、快捷键和托盘。
- 无默认附加插件。设置中的市场管理页支持主动安装、检测或更新 `dshmarket`；用户安装的市场在重启和升级后保留。
- 软件只检测 MyDSH 桌面发布，不再检测或安装上游 Harness 更新。
- 继续使用 `~/.minke` 数据目录，避免改名造成会话和设置丢失。

## 本次修复

- 设置打开时去掉标题栏底部未遮罩的 1px 边框，模糊遮罩连续覆盖标题栏。
- 设置面板在标题栏下方的可用区域居中，小窗口保留上下边距。
- 旧桌面版本把 React、Zod 和 Schemastery 直接登记在 profile 顶层，市场因此将运行库列为独立插件。迁移只移除已知旧规格的冗余登记，运行所需依赖仍由现有 bundle 或上游宿主管理，不盲目跨主版本升级。
- 同时清理已知旧 alpha UI/host 依赖登记，保留未知插件与用户自定义版本。
- MyDSH 新图标用于程序、安装器、启动页和两个主界面品牌图标。
- 修复 Windows 短路径、大小写和 junction 别名的事务路径判断；快照内链接按真实目标重定位，仍拒绝越界状态文件。

## 验证

- 223 项桌面回归通过，主进程、客户端及 host 类型检查通过。
- Windows x64 打包通过，真实 Electron 用现有用户目录启动通过。
- 设置在 1280x1000、1280x800、960x640 下的位置和标题栏遮罩检查通过。
- 实际 profile 迁移后三个冗余库登记消失，市场保留；市场管理页检测安装版本和最新版本均为 `1.46.1`。
- 不包含付费模型请求验证，也不宣称已完成所有干净 Windows 环境的安装演练。

## 运行与发布

系统需要 Node.js 24+、Corepack/pnpm 11.7.0 和 DSH 0.1.5-rc.2，安装包不嵌入这些依赖。
发布构建使用 `pnpm package` 和 `pnpm nsis:make`，安装器位于 `out/nsis`，随发布提供 SHA256SUMS。

后续优先验证干净 Windows 环境首次安装、桌面更新和卸载保留数据；上游升级由项目维护者验证并随桌面版本发布。
